# Design: Gigity — AI Video Workflow Orchestrator

Generated by /office-hours on 2026-04-10
Branch: main
Repo: NaKMiers/Gigity
Status: APPROVED
Mode: Startup

## Problem Statement

Solo app developers and indie makers (like the Deewas developer) need to create
advertising videos for TikTok, YouTube Reels, and Instagram Reels regularly. The
current process requires 5+ separate AI tools (ChatGPT, SunoAI, KlingAI, Midjourney/
DALL-E, CapCut) with no shared context, no memory between sessions, and no way to
maintain brand/character consistency across a campaign. Each video is a half-day of
tab-switching, re-explaining context, and manual assembly.

## Demand Evidence

- Builder has personally done this workflow multiple times for Deewas (deployed on
  App Store and Play Store)
- Pain is real: time (hours per video), money (multiple subscriptions), quality
  (inconsistent output with no templates or saved context)
- Landscape research confirms: "tool fragmentation" is the #1 persistent complaint
  among AI video creators in 2026, with no tool cleanly owning end-to-end workflow

## Status Quo (original manual flow)

The original 10-step manual process before Gigity:

1. Give ChatGPT business context — re-enter every session
2. Show ChatGPT example videos as reference
3. Ask ChatGPT for a story (gets text)
4. Ask ChatGPT to turn story into lyrics for SunoAI
5. Ask ChatGPT for a SunoAI prompt → generate music in SunoAI
6. Ask ChatGPT for character image prompts → generate in Midjourney/DALL-E
7. Per scene: ask ChatGPT for action image prompt → generate in image tool
8. Send each scene image to KlingAI → generate video clip
9. Assemble clips + music + logo in CapCut
10. Upload to TikTok, YouTube, Instagram Reels

Cost: ~4-6 hours per video. Multiple paid subscriptions. Zero context retention
between videos. Characters and brand visuals drift across videos.

## Gigity Workflow (V1 with LLM integration)

Gigity replaces steps 1-7 with direct LLM API calls using the user's OpenAI API key
(or any OpenAI-compatible endpoint). Steps 8-10 remain external.

```
Steps 1-7 → LLM API (OpenAI or compatible, user's own key)
Steps 8-10 → External tools (KlingAI, CapCut, manual upload)
```

## Target User & Narrowest Wedge

**Target user:** Solo developers and indie makers with a deployed product who run
their own social media marketing with no marketing team. They know their product deeply
but spend painful hours producing ad content.

**Narrowest wedge:** A single-product, brand-aware workflow tool. You enter your
product info once. It pre-populates all prompts for every step. You follow the workflow
in sequence. All context is saved. The output is a set of prompts + assets + a progress
tracker — you still use the AI tools, but you stop re-explaining and context-switching.

Note: V1 does NOT solve character visual consistency (Midjourney/DALL-E generates
different-looking characters even with the same prompt). V1 only provides a place to
save reference image URLs — the user still manually manages consistency.

## Constraints

- Solo developer, building during spare time
- Next.js 16 / React 19 / Tailwind CSS 4 — tech is already decided
- Bun as package manager
- Must be usable by the builder as day-one user (no external dependency risk)
- LLM API integration for steps 1-7: OpenAI API (or OpenAI-compatible endpoint), user's own key
- No SunoAI API, no KlingAI API in V1 — those remain external tools
- Infrastructure services (MongoDB) are allowed
- Note: Next.js 16, React 19, and Tailwind CSS 4 are all cutting-edge as of 2026;
  some ecosystem packages may not be compatible yet — check before adding dependencies

## Auth Scope

V1 is a personal tool used by one person. NextAuth is already configured in .env.example
but auth is **out of scope for V1** — run without authentication initially (single user,
local/personal deploy). Auth scope deferred to V2 when multi-user is needed.

## Premises

1. The core pain is the 10-step context-switch across 5+ AI tools to produce one video
2. The current workflow takes 4-6 hours per video and costs subscriptions to multiple services
3. The target user is solo builders/makers who need social video ads but have no marketing team
4. The builder (Deewas developer) is the archetypal user — building ads for their own app
5. The opportunity is orchestration: one place to run the whole workflow instead of juggling tabs
6. V1 is intentionally a single-feature product (brand profile + workflow stepper) — this is
   sufficient for the personal-use validation goal before investing in API integration

## Data Model

### BrandProfile (MongoDB collection: `brand_profiles`)

One brand profile per product. V1 treats the first (and only) profile as the active one.
V2 can add multi-brand support with an `isActive` flag.

```
{
  _id: ObjectId,
  name: string,                    // "Deewas"
  description: string,             // "Personal financial management app with AI"
  targetAudience: string,          // "Young adults managing their first salary"
  tone: string,                    // "Warm, encouraging, modern"
  platforms: string[],             // ["TikTok", "YouTube", "Instagram Reels"]
  exampleVideoUrls: string[],      // URLs of reference/example videos
  characters: Character[],         // see below
  logoUrl: string,                 // URL entered by user (no file upload in V1)
  createdAt: Date,
  updatedAt: Date
}

Character {
  name: string,                    // "Minh"
  description: string,             // "25-year-old office worker, friendly"
  referenceImageUrl: string        // URL of approved reference image
}
```

### VideoProject (MongoDB collection: `video_projects`)

```
{
  _id: ObjectId,
  brandProfileId: ObjectId,
  title: string,                   // "Deewas - March campaign video 1"
  status: "in_progress" | "completed",
  steps: WorkflowStep[],           // one per workflow step
  createdAt: Date,
  updatedAt: Date
}

WorkflowStep {
  stepNumber: number,              // 1-10
  conversation: Message[],         // full LLM conversation thread for this step
                                   // grows on retry/follow-up; empty for external steps
  llmResponse: string | null,      // latest assistant response = used as {{step_N_output}} context
  outputAssetUrl: string | null,   // for external steps: URL user pastes
  status: "pending" | "generating" | "done",
  completedAt: Date | null
}

Message { role: "user" | "assistant", content: string }
// System message (brand context + prior step outputs) recomputed at runtime — NOT stored.

// promptTemplate and generatedPrompt NOT stored in DB — computed on-the-fly from
// workflow-templates.ts + BrandProfile + activeCharacter snapshot at render time.

```

### Prompt Template Format

Templates use `{{variable}}` interpolation. Available variables from BrandProfile:

- `{{brand_name}}`, `{{brand_description}}`, `{{target_audience}}`, `{{tone}}`
- `{{character_name}}`, `{{character_description}}`, `{{character_reference}}`
- `{{platform}}`, `{{example_videos}}`

Base templates are hardcoded in V1 (not user-editable). Templates live in
`/src/lib/workflow-templates.ts` as a constant array of 10 step definitions.

## Screens

1. **Brand Setup** (`/brand/new`, `/brand/[id]/edit`)
   - Form: name, description, target audience, tone, platforms, logo URL (text input — no upload)
   - Character management: add/remove characters with name, description, reference image URL
   - Example video URL list

2. **Dashboard** (`/`)
   - If no brand profile exists: redirect to `/brand/new`
   - If brand profile exists: show list of video projects (most recent first)
   - "New Video" button → create VideoProject linked to the single brand profile
   - Status badge per project (in_progress / completed)
   - "Edit Brand" link → `/brand/[id]/edit`

3. **Workflow View** (`/projects/[id]`)
   - Single page. Step 1 auto-generates on page load (no click needed).
   - Sidebar: step 1-10 with icons (pending / generating / active / done)
   - Main panel — `llm` step states:
     - `generating`: spinner + "Generating..."
     - `has result`: response card (read-only) + follow-up input + Retry + Approve
     - `done`: response card (read-only) + "✓ Approved"
   - Main panel — `external_instruction` step: instruction card + asset URL input + Approve
   - **Approve**: POST .../approve → step done → step N+1 becomes active (user still clicks Generate)
   - **Retry**: clears conversation, fresh LLM call
   - **Follow-up + send**: appends to conversation[], sends full thread to LLM
   - Done steps in sidebar: clickable, show approved response read-only
   - No `/projects/[id]/step/[n]` routes

## Milestones

- **M1** (Days 1-3): Brand profile CRUD — form, MongoDB schema, basic list view
- **M2** (Days 4-7): Workflow template engine — `workflow-templates.ts` (10 step defs, 7 LLM prompts validated), variable interpolation, VideoProject creation, POST /api/v1/llm (plain fetch to OpenAI)
- **M3** (Days 8-10): Workflow stepper UI — step cards, "Generate" button with loading state, streaming response textarea, mark done, progress bar; external step instruction cards
- **M4** (Day 11-14): Use it for real — produce one Deewas video end-to-end using Gigity

## Approaches Considered

### Approach A: Workflow Orchestrator with LLM API (CHOSEN)

One UI around the 10-step workflow. Steps 1-7 call LLM API directly (user's own OpenAI key).
Steps 8-10 remain external. Save brand context once. Track progress per video.

- Effort: S+ (1-2 weeks solo)
- Risk: Low — user's own API key, no Gigity billing complexity
- Ships: immediately usable on day 1, faster than manual copy-paste

### Approach B: Full API Pipeline (deferred to V2)

Full LLM + SunoAI API + KlingAI API integration.

- Effort: L (1-2 months)
- Risk: High (SunoAI/KlingAI API access, credit costs)

### Approach C: Brand Intelligence Layer

Product profile that generates prompts for any AI tool. Tool-agnostic. Rejected:
feels like a feature, not a product — and Approach A already includes this as its core.

## Recommended Approach

**Approach A — Workflow Orchestrator with LLM API**

Why: Calling the LLM API directly eliminates the copy-paste step entirely — the biggest
daily friction point. User brings their own OpenAI key, so there's no billing complexity
on Gigity's side. The LLM integration is one POST endpoint using plain fetch — not a
significant complexity increase over the pure orchestrator approach.

Steps 8-10 (KlingAI, CapCut, upload) stay external because those tools have no clean API.

## Open Questions (V1 answers)

1. **Campaign concept?** V1 answer: out of scope. Each VideoProject is standalone.
   Campaign grouping is a V2 feature.
2. **Character visual consistency?** V1 answer: not solved. Save reference image URLs
   in brand profile; user manually manages consistency across image generation sessions.
3. **Pricing?** Out of scope until V2 (multi-user SaaS). No pricing decisions needed now.
4. **CapCut integration?** Out of scope until V2. V1 treats CapCut as an external step
   with no integration.

## Success Criteria

- Builder produces a Deewas ad using Gigity in under 2 hours (vs. current 4-6)
- Brand context saved across sessions — zero re-entering of product info
- 10-step workflow surfaced as clear UI steps with copy-paste prompts
- At least 3 videos produced before evaluating whether to add API integration

## Distribution Plan

V1: Personal tool, local deploy only. No distribution needed.
V2 (if validated): Next.js app deployed on Vercel — existing `next build` + Vercel CI
pipeline. Public URL for sharing with other indie makers.

## Dependencies

- Next.js 16.1.1, React 19, Tailwind CSS 4 (already installed — note cutting-edge versions)
- MongoDB for brand profiles + video project storage — add `mongoose` + `server-only` via bun
- LLM API: OpenAI chat completions via plain `fetch` — `OPENAI_API_KEY` in env vars (server-only)
  - `LLM_BASE_URL` optional for OpenAI-compatible endpoints (Ollama, Together AI, etc.)
  - `LLM_MODEL` optional override (default: `gpt-4o-mini`)
- No SunoAI API, no KlingAI API in V1
- NextAuth deferred to V2

## The Assignment

Before writing a single component, write the exact LLM prompt (system + user message)
for each of the 7 LLM steps — word for word, for a hypothetical Deewas campaign.
Test each prompt against the OpenAI API manually (curl or Playground) before wiring it
into the app. Also write the instruction text for the 3 external steps (8-10).

These 10 step definitions are your `workflow-templates.ts`. M1 starts only after all
10 are written and the 7 LLM prompts are validated manually.

## What I noticed about how you think

- You described your workflow in exact, numbered steps in the README. That's not something
  most founders do. It means you're already thinking like a product person — the workflow
  IS the product, before any code exists.
- When asked who else needs this, you said "small business owners" — a category. But the
  user you described when you talked about Deewas? That's a person. You know that user.
  Trust that instinct. The indie developer who builds their own app and their own marketing
  IS the specific user. You are them.
- You chose Approach A (smallest working version) over Approach B (full automation)
  without hesitation. That's a good signal. Founders who want to build the full platform
  before anyone uses it usually haven't talked to enough users yet.

